{
  "devDependencies": {
    "cypress": "^13.5.0",
    "standard-version": "^9.5.0"
  },
  "version": "1.1.1",
  "scripts": {
    "release": "standard-version"
  },
    "scripts": {
      "cypress:run": "cypress run --headless"
    }
}
